create database chapter3_ex4;
use chapter3_ex4;

create table teacher (
id int primary key,
name varchar(255) not null
);

create table student (
id int primary key,
name varchar(255) not null,
teacher_id int,

constraint FK_teacher_student foreign key (teacher_id)
references teacher(id)
);